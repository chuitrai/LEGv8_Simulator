package main.java.com.mydomain.legv8simulator.simulator.pipeline;

/**
 * Lớp trừu tượng cơ sở cho tất cả các chốt (latch) trong pipeline.
 * Hiện tại để trống, đóng vai trò là một kiểu chung để định danh.
 */
public abstract class PipelineLatch {
    // Có thể thêm các thuộc tính chung sau này nếu cần
    // ví dụ: mã lệnh hoặc PC của lệnh đang được xử lý.
    public void print() {
        // Stub method for printing latch information
    }
}