package main.java.com.mydomain.legv8simulator.core;

public enum ALUOperation {
    ADD,
    SUBTRACT,
    AND,
    OR,
    XOR,
    LOGICAL_SHIFT_LEFT,
    LOGICAL_SHIFT_RIGHT,
    ARITHMETIC_SHIFT_RIGHT,
    SIGNED_DIVIDE,
    UNSIGNED_DIVIDE,
    PASS_THROUGH_B // Một phép toán đặc biệt để chỉ chuyển tiếp toán hạng B
}
